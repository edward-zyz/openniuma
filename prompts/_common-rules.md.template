<!-- 拷贝为 _common-rules.md，填入你的项目规范 -->

## 硬性红线
- 禁止合并代码到 {{main_branch}}
- 禁止请求用户输入或等待用户操作（非交互模式）
- 全程自主工作，不要问我问题

## Worktree 安全约束
- **禁止 `cd` 到主仓库目录再执行 git 命令**（会破坏主仓库分支状态）
- **禁止 `git -C <主仓库路径> checkout ...`**（同上）

## 硬性门禁（每步必须全部 exit 0）
```bash
{{gate_command}}
```

<!-- 在下面添加你的项目特定规范（环境预检、时间格式、UI 规范等） -->
